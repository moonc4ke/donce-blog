<div class="nav-menu" data-controller="menu" data-menu-target="menu">
  <div>
    <button
      class="menu-button menu-button--primary"
      data-menu-target="button"
      data-action="click->menu#toggle"
    >
      MENU
    </button>

    <%= link_to "donce.dev", root_path, class: "main-page-link underlined-link" %>
  </div>

  <div class="nav-menu__links" data-menu-target="links">
    <%= link_to "Home",
    root_path,
    class:
      "nav-menu__link underlined-link #{current_page?(root_path) ? "nav-menu__link--active" : ""}" %>

    <%= link_to "Projects",
    projects_path,
    class:
      "nav-menu__link underlined-link #{current_page?(projects_path) ? "nav-menu__link--active" : ""}" %>

    <% if authenticated? %>
      <%= link_to "New blog post",
      new_blog_post_path,
      class:
        "nav-menu__link underlined-link #{current_page?(new_blog_post_path) ? "nav-menu__link--active" : ""}" %>

      <%= link_to "Log out",
      session_path,
      method: :delete,
      data: {
        turbo_method: :delete,
      },
      class: "nav-menu__link underlined-link" %>
    <% end %>
  </div>
</div>
